'use client'
import { motion } from 'framer-motion'
import ValidatorCounter from '../components/ValidatorCounter'

export default function Home() {
  return (
    <div className="relative flex flex-col items-center justify-center min-h-[80vh] p-8 text-center">
      <motion.h1 
        initial={{ opacity: 0, y: 50 }}
        animate={{ opacity: 1, y: 0 }}
        className="text-6xl md:text-8xl font-bold mb-8 text-purple-400"
      >
        Forever Couch Gang
      </motion.h1>
      <motion.p 
        initial={{ opacity: 0, y: 30 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ delay: 0.2 }}
        className="text-xl md:text-2xl mb-12 max-w-2xl"
      >
        The Blockchain for Couch Rotters. Earn $FCGC by watching, sharing, and decaying in peace.
      </motion.p>
      <ValidatorCounter />
      <motion.button 
        whileHover={{ scale: 1.05 }}
        whileTap={{ scale: 0.95 }}
        className="mt-8 px-8 py-4 bg-purple-600 hover:bg-purple-700 rounded-full text-lg font-bold"
      >
        Join the Rot – Discord
      </motion.button>
    </div>
  )
}